#include<stdio.h>
#include<stdlib.h>
int union(){
    int i,j,temp,x;
    printf("pleas enter  size of array");
    scanf("%d"&x);
    int A[x];
    int B[x];
    for ( i = 0; i < x; i++)
    {
        printf("[%d]")
    }
    
}